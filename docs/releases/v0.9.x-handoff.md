# v0.9.x → v1.0-alpha 交接追蹤

> 供後續 Agent 接手。每完成一版更新本表與 `learning-log-v1-prep.md`。  
> **停止線：v1.0-alpha 前**（不開 Trust Tier runtime change）。

## 規則

1. **一版一 PR**：`cursor/v09N-<name>-d691` → base `develop`；驗收通過才 merge。
2. 每個 patch 對應一個 OpenSpec change；完成後 sync → validate → archive。
3. 發版：`release/0.9.N` 只做版本 bump（`pyproject.toml` + `__init__.py`），merge `main` + tag，再 `--no-ff` 回 `develop`。
4. 合併前必過：`./scripts/check.sh`。
5. 每版追加學習紀錄；更新本表狀態。

## 進度

| 版本 | Change | 分支 | PR | develop merge | release/tag | 狀態 |
|------|--------|------|-----|-------------|-------------|------|
| prep docs | `v1-prep` (archived) | 隨 v0.9.1 | — | ✅ | — | 完成 |
| v0.9.1 | `shell-containment` (archived) | `cursor/v091-shell-containment-d691` | #8 | ✅ | ✅ `v0.9.1` | 完成 |
| v0.9.2 | `policy-middleware` (archived) | `cursor/v092-policy-middleware-d691` | #9 | ✅ | ✅ `v0.9.2` | 完成 |
| v0.9.3 | `policy-trust-boundary` (archived) | `cursor/v093-policy-trust-boundary-d691` | TBD | ✅ | ✅ `v0.9.3` | 完成 |
| v0.9.4 | `audit-integrity` (archived) | `cursor/v094-audit-integrity-d691` | TBD | ✅ | ✅ `v0.9.4` | 完成 |
| v0.9.5 | `principal-scope` (archived) | `cursor/v095-principal-scope-d691` | TBD | ✅ | ✅ `v0.9.5` | 完成 |
| v0.9.6 | `session-auth` (archived) | `cursor/v096-session-auth-d691` | #13 | ✅ | ✅ `v0.9.6` | 完成 |
| v0.9.7 | `trust-grant-store` (archived) | `cursor/v097-trust-grant-store-d691` | TBD | ✅ | ✅ `v0.9.7` | 完成 |
| v0.9.8 | `trust-decision-matrix` (archived) | `cursor/v098-trust-decision-matrix-d691` | TBD | ✅ | ✅ `v0.9.8` | 完成 |
| v0.9.9 | `evidence-closure` (archived) | `cursor/v099-evidence-closure-d691` | TBD | ✅ | ✅ `v0.9.9` | 完成 |
| v1.0-alpha | `trust-framework` | — | — | — | — | **停止線：不由本序列實作** |

## 接手檢查清單

```bash
git fetch origin --tags
git checkout develop && git pull origin develop
cat docs/releases/v0.9.x-handoff.md
tail -n 60 docs/releases/learning-log-v1-prep.md
./scripts/check.sh
```

v0.9.x 已沒有下一個 patch change；讀 `learning-log-v1-prep.md`、`docs/releases/v1.0-alpha-known-issues.md` 與 v0.9.9 evidence 後，在滿足 admission gate 時另開 v1.0-alpha change。

## 單一 patch 標準作業（給下一個 Agent）

1. `git checkout develop && git pull && git checkout -b cursor/v09N-<change>-d691`
2. OpenSpec：`npx @fission-ai/openspec@latest new change "<change>"` → proposal／design／specs／tasks（**Non-goals 必寫**）
3. 依 tasks 漸進實作；每階段 `uv run pytest`
4. sync delta → `validate --specs --strict` → archive → `./scripts/check.sh`
5. push 分支；用 `ManagePullRequest` 開 PR（base `develop`）
6. 驗收通過後：`bash scripts/release-patch.sh cursor/v09N-<change>-d691 N "<summary>"`
7. 更新本表與 learning-log；**停止於 v1.0-alpha 前**（勿開 Trust Tier runtime）

已完成 tag：`v0.9.1`（shell containment）、`v0.9.2`（middleware）、`v0.9.3`（policy trust boundary）、`v0.9.4`（audit integrity）。

## v0.9.1 交接狀態

- Runtime／regression evidence：[`v0.9.1-shell-containment-evidence.md`](v0.9.1-shell-containment-evidence.md)；V1-001／V1-002 已在 implementation revision 關閉。
- Compatibility break：raw `python`、`uv run`、shell interpreter、未知 executable、compound shell、expansion 與未建模 options 現在 fail-closed；pytest 走 typed `run_tests`。
- Release branch 待辦：只有 `release/0.9.1` 可更新 `pyproject.toml` 與 `src/council_agent/__init__.py`，兩處需一致；本 feature branch不得 bump 或 tag。
- v0.9.2 仍從 `policy-middleware`／唯一 dispatcher／`SecurityContext` 開始。不得把 v0.9.1 的 shell-local prepared action helper 宣稱成統一 middleware。
- v1.0-alpha Trust Tier runtime、`council trust` 與 tier CLI 仍是停止線後工作，v0.9.x 不提前實作。

## v0.9.2 交接狀態

- Runtime／regression evidence：[`v0.9.2-policy-middleware-evidence.md`](v0.9.2-policy-middleware-evidence.md)；SEC-P0-002／V1-003 已由唯一 `invoke()`、單一 `SecurityContext` lifecycle、direct／Crew bypass tests 與 315-test regression 關閉。
- Public library breaking change：六個 public tools 需要 active `SecurityContext`；缺少／closed／stale context 回傳 structured refusal，不再 ambient fail-open。直接 caller 使用 `SecurityContext.create(...)` + `security_context(...)`。
- CrewAI adapters 不再接 tracker/session，也不做 limit/session/audit；middleware 是唯一 evidence writer。Sandbox 每個 action 產生一個 tracker summary、一個 session line與 attempt/result 兩筆 audit。
- 無 sandbox 仍套用 context/workspace/policy/confirmation/limit 並保留 in-memory summaries，但沒有 durable session/audit；不能把 audit 空檔解讀成沒有 action。
- `policy_version="v0.9-unversioned"` 只是 snapshot label。v0.9.3 必須接續 `policy-trust-boundary`，處理 project policy restrict-only trust boundary、versioned schema 與未知欄位 fail-fast，不得在 v0.9.2 偷做。
- v0.9.4 audit integrity、v0.9.5 principal、v0.9.6 authentication、v0.9.7 grant、v0.9.8 decision matrix 與 v1.0-alpha Trust Tier runtime 均未提前實作。
- Release branch 待辦：只有 `release/0.9.2` 可更新 `pyproject.toml` 與 `src/council_agent/__init__.py`，兩處需一致；本 feature branch不得 bump 或 tag。

## v0.9.3 交接狀態

- Runtime／regression evidence：[`v0.9.3-policy-trust-boundary-evidence.md`](v0.9.3-policy-trust-boundary-evidence.md)；SEC-P0-004／V1-004 已由 strict `schema_version: 1`、unknown／typo fail-fast、restrict-only precedence、versioned context snapshot 與 policy-file sentinel tests 關閉。
- Breaking migration：既有 unversioned `council.policy.yaml` 必須新增 integer `schema_version: 1`，並移除 `allowed_commands`／`denied_commands`／`denied_paths` 以外的所有欄位。String `"1"`、future version、`trust_tier`、`grant`、`scope`、拼錯及任意未知欄位會拒絕整份檔案，不再忽略。
- Project policy 只作限制：allowlist 是 built-in／後續 gates 的 intersection，deny/path 只作 union；不能建立 principal、authentication、grant、Trust Tier 或 confirmation bypass。`policy_version` snapshot label 是 `builtin` 或 `project-policy/v1`。
- Root／nested `council.policy.yaml` 是 built-in denied path；public filesystem tools 與受支援 shell path action 不能直接讀寫／刪除，且拒絕無副作用。這不是 OS sandbox：`run_tests` 中的專案程式碼、host user 或外部程序仍可能改檔。
- v0.9.4 接續 audit control-plane／redaction／sequence；workspace 外 user-owned trust grant store 仍只屬 v0.9.7。不得將 project policy 當 grant store，也不得提前實作 Trust Tier。
- OpenSpec delta 已同步並歸檔至 `openspec/changes/archive/2026-08-11-policy-trust-boundary/`；17/17 tasks complete。
- Post-archive `./scripts/check.sh`：334 passed；no active changes；main specs 5/5 strict passed。
- Release branch 待辦：只有 `release/0.9.3` 可更新 `pyproject.toml` 與 `src/council_agent/__init__.py`，兩處需一致；本 feature branch不得 bump 或 tag。

## v0.9.4 交接狀態

- Runtime／regression evidence：[`v0.9.4-audit-integrity-evidence.md`](v0.9.4-audit-integrity-evidence.md)；V1-005／SEC-P0-007／SEC-P1-003 的 v0.9.4 substrate scope由 control-plane deny、recursive redaction、schema-v1 sequence/canonical ID及 exact attempt/result linkage關閉。
- Root／nested `.council` 與 project policy是 built-in denied paths；public filesystem與受支援 shell path action不可將 audit/session/config/reserved auth/grant control plane當作 target。Project policy不能移除；POSIX mode只作 defense in depth。
- Audit/session persistence共用 sensitive-key與 common-token content redaction，且 redaction早於 truncation。Legacy evidence可 sanitized read/export但 status固定 `legacy_unverified`。
- New audit event含 `schema_version: 1`、serialized sequence、content-derived `sha256:` event ID；strict load偵測 gap、duplicate、reorder、partial/truncated line、field mutation及 broken attempt/result correlation。CLI顯示 integrity status並拒絕 invalid export。
- 這不是完整 hash chain：沒有 predecessor link、external signature/checkpoint、immutable sink或 tail/whole-log replacement保證。Host owner/root、`run_tests` project code與 external process不受 product path deny containment。
- 漸進測試：audit 23 passed；guard/shell/session 101 passed；middleware/CLI/e2e 41 passed。Full／active-change `./scripts/check.sh`：375 passed；changes strict 1/1；main specs strict 5/5。Archive path待完成後回填。
- v0.9.5 從 `principal-scope` 開始：provider API key不是 authorization principal；不得把 v0.9.4 event/session ID當 authentication、scope或 grant。
- Release branch 待辦：只有 `release/0.9.4` 可更新 `pyproject.toml` 與 `src/council_agent/__init__.py`，兩處需一致；本 feature branch不得 bump 或 tag。

## v0.9.5 交接狀態

- Runtime／regression evidence：[`v0.9.5-principal-scope-evidence.md`](v0.9.5-principal-scope-evidence.md)；V1-006已由typed provider/principal boundary、closed cumulative scopes、per-action current authority與masked `scope_authorization` evidence關閉。
- `OPENROUTER_API_KEY`只形成`OpenRouterCredential`並傳入model builders；Council `Principal`另有stable ID、kind、issuer與scopes。`run_council`／direct `SecurityContext` caller必須顯式提供principal，provider key或session ID不授權tools。
- Scope matrix：read tools需`read`；filesystem mutate需`filesystem:mutate`；`run_tests`需`test`+mutate；shell read/write/dangerous需`shell`並分別累加read／mutate／mutate+`high-risk:manage`。
- Missing／invalid／identity mismatch／scope不足／resolver revoke在handler前fail closed。Read-only、test-only與mutate-only principal都不能藉`run_tests`、Crew wrapper、project policy、confirmation或`--yes` mutate；no-process與sentinel tests已覆蓋。
- Audit/session只保存masked principal ref與scope decision，不保存raw principal ID／provider credential；authorization metadata沿用schema-v1 envelope，v0.9.4 event canonical compatibility保留。
- CLI local principal是authorization declaration，不是authentication。v0.9.6必須接續`session-auth`，不可把local settings、session UUID或`--yes`當step-up；persistent user-owned grant/revoke仍只屬v0.9.7。
- 漸進測試：principal core 22 targeted／397 full；dispatcher 51 targeted／405 full；orchestration 61 targeted／411 full；wrapper/composite bypass 19 targeted／422 full。Final active/post-archive gate見evidence。
- OpenSpec delta同步並歸檔至`openspec/changes/archive/2026-08-11-principal-scope/`；28/28 tasks complete。
- Release branch待辦：只有`release/0.9.5`可更新`pyproject.toml`與`src/council_agent/__init__.py`，兩處需一致；本feature branch不得bump或tag。

## v0.9.6 交接狀態

- Runtime／regression evidence：[`v0.9.6-session-auth-evidence.md`](v0.9.6-session-auth-evidence.md)；V1-007已由process-local challenge/token lifecycle、exact binding、freshness、revoke/replay、product high-risk gate與masked audit關閉。
- Session UUID仍只是correlation。Challenge/token另綁masked principal、canonical workspace、runtime session、purpose、exact action與expiry；failed/mismatched proof在判定前consume，restart不恢復outstanding state。
- Product orchestrator固定`require_high_risk_step_up=True`；scope需`high-risk:manage`的action在policy/confirmation/handler前必須fresh authentication。`--yes`只選confirmation auto，沒有`COUNCIL_AUTH_SECRET`仍以`authentication_missing`拒絕且無process/filesystem副作用。
- CLI/service verifier以獨立`SecretStr`載入，不提供command-line secret option。Audit lifecycle涵蓋success/failure/expiry/revocation/replay；audit/session/console/evidence只保留masked refs，不保存verifier、challenge、nonce、response或token。
- Direct library context為相容性預設不啟用新gate；caller可顯式設定`require_high_risk_step_up=True`與manager/provider。這不表示default context已authenticated。
- OpenSpec delta同步並歸檔至`openspec/changes/archive/2026-08-11-session-auth/`；25/25 tasks complete。Active/post-archive gate均為459 passed；archive後active changes 0、main specs 5/5 strict passed（見evidence）。
- v0.9.7從`trust-grant-store`開始：process-local manager不是workspace外user-owned persistent store，revoke不跨restart持久化。不得提前映射Trust Tier runtime。
- Release branch待辦：只有`release/0.9.6`可更新`pyproject.toml`與`src/council_agent/__init__.py`，兩處需一致；本feature branch不得bump或tag。

## v0.9.7 交接狀態

- Runtime／regression evidence：[`v0.9.7-trust-grant-store-evidence.md`](v0.9.7-trust-grant-store-evidence.md)；V1-008由workspace外 user-owned strict schema store、exact principal/action/resource/scope binding、authenticated management、atomic update、persistent revoke與masked audit關閉。
- Store預設`${XDG_DATA_HOME:-~/.local/share}/council-agent/trust/`；POSIX current UID、non-symlink、workspace non-overlap、dir `0700`／file `0600`、safe ancestor與lock任一不可確認即fail closed。Project policy、project `.env`與Agent workspace write不是授權來源。
- Grant是self-bound exact record，scope不得超過current principal；grant/revoke需`high-risk:manage`、list需`read`及principal/store/session/purpose/exact-arguments一次性fresh authentication。Provider key、session ID、confirmation、`--yes`均不能滿足。
- Revoke tombstone／expiry在下一個locked lookup立即生效並跨restart。Malformed/future/unknown schema、unknown fields、duplicate/conflicting records整份拒絕，不做partial adoption／auto migration。
- Mutation使用validated no-follow directory/lock、exclusive inter-process lock、same-directory temp、file/directory `fsync`與atomic replace；concurrent writer與partial-write/replace sentinel皆通過。Trust audit只含masked identity/target/grant refs，不含raw principal/resource/grant ID/verifier/proof。
- `council trust grant/revoke/list`是provider-independent store管理基礎；不提供`--trust-tier`／`--yes`，也不把grant接入`SecurityContext`或middleware。Stored grant不會改變現行scope/policy/authentication/confirmation decision。
- 漸進測試：store 25 focused／484 full；authentication+audit 32 focused／491 full；CLI 7 focused／498 full。Active/post-archive `./scripts/check.sh`均為498 passed；archive後active changes 0、main specs 5/5 strict passed。
- OpenSpec security／orchestration delta已sync；25/25 tasks complete；archive為`openspec/changes/archive/2026-08-11-trust-grant-store/`。
- v0.9.8只能接續`trust-decision-matrix`定義precedence；仍不得啟用Trust Tier 0/1/2 runtime或`--trust-tier`。
- Release branch待辦：只有`release/0.9.7`可更新`pyproject.toml`與`src/council_agent/__init__.py`，兩處需一致；本feature branch不得bump或tag。

## v0.9.8 交接狀態

- Runtime／regression evidence：[`v0.9.8-trust-decision-matrix-evidence.md`](v0.9.8-trust-decision-matrix-evidence.md)；V1-009／SEC-P0-005由schema-v1 closed vector、policy→scope→authentication→grant→risk→interaction precedence、stable reason及dispatcher-owned evidence關閉。
- 純`DecisionVector`只接受policy/scope/authentication/grant/risk/interaction六維closed enum；結果只可`deny`／`require_confirmation`／`allow`。Simultaneous denials固定回最前gate，AUTO／approved不能覆蓋任何authority denial。
- `ConfirmMode`相容行為不變但語意固定為interaction only。CLI `--yes`只解析成AUTO；help明示不grant scope、authenticate、create trust grant或elevate privilege。Non-TTY無yes仍REFUSE。
- Mandatory dispatcher新增`metadata.trust_decision`；direct library、Crew result audit、tracker/session/result audit使用同一normalized object。普通operation error不誤標authority deny。
- Product runtime grant固定`trust_grant_not_required`且不讀`TrustGrantStore`。沒有Tier enum、Tier 0/1/2 context/runtime或`--trust-tier`；matching v0.9.7 record仍不影響tool。
- 漸進測試：pure 44 focused／542 full；dispatcher 51 focused／549 full；CLI/Crew/library 55 focused／553 full。Active `./scripts/check.sh`為553 passed、change 1/1、main specs 5/5；post-archive亦為553 passed、active changes 0、main specs 5/5。
- OpenSpec security／tools／orchestration delta已sync；21/21 tasks complete；archive為`openspec/changes/archive/2026-08-11-trust-decision-matrix/`。
- v1.0-alpha仍是停止線；下一版只能接v0.9.9 `evidence-closure`。未來tier change必須另加typed source、explicit grant lookup與tier translator，不得改matrix-v1 deny precedence。
- Release branch待辦：只有`release/0.9.8`可更新`pyproject.toml`與`src/council_agent/__init__.py`，兩處需一致；本feature branch不得bump或tag。

## v0.9.9 交接狀態

- Runtime／regression evidence：[`v0.9.9-evidence-closure-evidence.md`](v0.9.9-evidence-closure-evidence.md)；V1-010／SEC-P1-002 由 bounded escalation re-verification、ordered attempt history、current-attempt tool/test gate 與 result/tracker/session/audit correlation 關閉。
- Initial execution與每次escalation有唯一`pipeline_attempt_id`；每次用同一plan／success criteria重新Verification。`max_retries`耗盡後final verdict保持FAIL，不能以新output搭配舊verdict。
- `CouncilResult`的final execution／output／verdict／tool summaries／attempt ID只選`attempts[-1]`；舊attempt execution、verdict與durable session/audit evidence保留。Tracker limit仍在整個run累計。
- Verification evidence floor對明確product tool與pytest語意fail closed；required `run_tests`須current attempt的`success=True`、`exit_code=0`、`failed=0`。這是保守lexical rule，不是任意語意形式證明。
- Escalation使用既有dispatcher-backed Execution Crew tools與同一`SecurityContext`；沒有第二條policy/authority path。
- V1-011文件矛盾已校正：公開手冊區分current candidate與歷史v0.9.0，並明示非OS sandbox、`run_tests` project code、無external audit anchor、grant尚未接runtime及Trust Tier停止線。獨立真人／無脈絡Agent走查仍是alpha admission gate，feature branch不假裝已完成該外部執行。
- OpenSpec orchestration／release-prep／sandbox／security delta已sync；archive為`openspec/changes/archive/2026-08-11-evidence-closure/`。Active gate為568 passed、change 1/1、main specs 5/5；post-archive gate待執行。
- Release branch待辦：只有`release/0.9.9`可更新`pyproject.toml`與`src/council_agent/__init__.py`並tag；本feature branch維持兩處`0.9.8`。

**v0.9.1–v0.9.9 完成；alpha admission 證據已由 Cloud Agent 代跑（2026-08-11）**

## 停止線（v0.9.x 清債）→ 下一序列

**v0.9.1–v0.9.9 清債序列已全部完成並打 tag。**

| 閘門 | 狀態 | 證據 |
|------|------|------|
| P0／P1 closed | ✅ | `v1.0-alpha-known-issues.md` |
| `./scripts/check.sh` | ✅ 568 passed | `evidence/v1.0.0-alpha.1/check.sh.log` |
| Smoke SMK-00～09 | ✅ | `evidence/v1.0.0-alpha.1/smoke-test.md` |
| Agent checklist | ✅ | `evidence/v1.0.0-alpha.1/agent-manual-run.md` |
| 獨立真人手冊 | **ACCEPTED**（Cloud 環境無獨立真人；使用者核准選項 1） | `evidence/v1.0.0-alpha.1/human-manual-status.md` |

下一動作：另開分支 `cursor/trust-framework-fce3` + OpenSpec change `trust-framework` 實作 **v1.0-alpha Trust Tier runtime**，再到 `v1.0.0-beta.1`。

接手 Agent 請先讀：
1. 本檔進度表與 admission 表
2. `docs/releases/learning-log-v1-prep.md` 尾端
3. `docs/releases/v1.0-alpha-known-issues.md`
4. `docs/releases/evidence/v1.0.0-alpha.1/`
5. `./scripts/check.sh` 必須全綠
