# v0.9.x → v1.0-alpha 交接追蹤

> 供後續 Agent 接手。每完成一版更新本表與 `learning-log-v1-prep.md`。  
> **停止線：v1.0-alpha 前**（不開 Trust Tier runtime change）。

## 規則

1. **一版一 PR**：`cursor/v09N-<name>-d691` → base `develop`；驗收通過才 merge。
2. 每個 patch 對應一個 OpenSpec change；完成後 sync → validate → archive。
3. 發版：`release/0.9.N` 只做版本 bump（`pyproject.toml` + `__init__.py`），merge `main` + tag，再 `--no-ff` 回 `develop`。
4. 合併前必過：`./scripts/check.sh`。
5. 每版追加學習紀錄；更新本表狀態。

## 進度

| 版本 | Change | 分支 | PR | develop merge | release/tag | 狀態 |
|------|--------|------|-----|-------------|-------------|------|
| prep docs | `v1-prep` (archived) | 隨 v0.9.1 | — | ✅ | — | 完成 |
| v0.9.1 | `shell-containment` (archived) | `cursor/v091-shell-containment-d691` | #8 | ✅ | ✅ `v0.9.1` | 完成 |
| v0.9.2 | `policy-middleware` (archived) | `cursor/v092-policy-middleware-d691` | #9 | ✅ | ✅ `v0.9.2` | 完成 |
| v0.9.3 | `policy-trust-boundary` (archived) | `cursor/v093-policy-trust-boundary-d691` | TBD | ✅ | ✅ `v0.9.3` | 完成 |
| v0.9.4 | `audit-integrity` (archived) | `cursor/v094-audit-integrity-d691` | TBD | ✅ | ✅ `v0.9.4` | 完成 |
| v0.9.5 | `principal-scope` | TBD | — | ❌ | ❌ | 未開始 |
| v0.9.6 | `session-auth` | TBD | — | ❌ | ❌ | 未開始 |
| v0.9.7 | `trust-grant-store` | TBD | — | ❌ | ❌ | 未開始 |
| v0.9.8 | `trust-decision-matrix` | TBD | — | ❌ | ❌ | 未開始 |
| v0.9.9 | `evidence-closure` | TBD | — | ❌ | ❌ | 未開始 |
| v1.0-alpha | `trust-framework` | — | — | — | — | **停止線：不由本序列實作** |

## 接手檢查清單

```bash
git fetch origin --tags
git checkout develop && git pull origin develop
cat docs/releases/v0.9.x-handoff.md
tail -n 60 docs/releases/learning-log-v1-prep.md
./scripts/check.sh
```

下一版從上表第一個「未開始／進行中」列開始；讀對應 `learning-log-v1-prep.md` 的版本章節與 `docs/releases/v1.0-alpha-known-issues.md`。

## 單一 patch 標準作業（給下一個 Agent）

1. `git checkout develop && git pull && git checkout -b cursor/v09N-<change>-d691`
2. OpenSpec：`npx @fission-ai/openspec@latest new change "<change>"` → proposal／design／specs／tasks（**Non-goals 必寫**）
3. 依 tasks 漸進實作；每階段 `uv run pytest`
4. sync delta → `validate --specs --strict` → archive → `./scripts/check.sh`
5. push 分支；用 `ManagePullRequest` 開 PR（base `develop`）
6. 驗收通過後：`bash scripts/release-patch.sh cursor/v09N-<change>-d691 N "<summary>"`
7. 更新本表與 learning-log；**停止於 v1.0-alpha 前**（勿開 Trust Tier runtime）

已完成 tag：`v0.9.1`（shell containment）、`v0.9.2`（middleware）、`v0.9.3`（policy trust boundary）、`v0.9.4`（audit integrity substrate）。

## v0.9.1 交接狀態

- Runtime／regression evidence：[`v0.9.1-shell-containment-evidence.md`](v0.9.1-shell-containment-evidence.md)；V1-001／V1-002 已在 implementation revision 關閉。
- Compatibility break：raw `python`、`uv run`、shell interpreter、未知 executable、compound shell、expansion 與未建模 options 現在 fail-closed；pytest 走 typed `run_tests`。
- Release branch 待辦：只有 `release/0.9.1` 可更新 `pyproject.toml` 與 `src/council_agent/__init__.py`，兩處需一致；本 feature branch不得 bump 或 tag。
- v0.9.2 仍從 `policy-middleware`／唯一 dispatcher／`SecurityContext` 開始。不得把 v0.9.1 的 shell-local prepared action helper 宣稱成統一 middleware。
- v1.0-alpha Trust Tier runtime、`council trust` 與 tier CLI 仍是停止線後工作，v0.9.x 不提前實作。

## v0.9.2 交接狀態

- Runtime／regression evidence：[`v0.9.2-policy-middleware-evidence.md`](v0.9.2-policy-middleware-evidence.md)；SEC-P0-002／V1-003 已由唯一 `invoke()`、單一 `SecurityContext` lifecycle、direct／Crew bypass tests 與 315-test regression 關閉。
- Public library breaking change：六個 public tools 需要 active `SecurityContext`；缺少／closed／stale context 回傳 structured refusal，不再 ambient fail-open。直接 caller 使用 `SecurityContext.create(...)` + `security_context(...)`。
- CrewAI adapters 不再接 tracker/session，也不做 limit/session/audit；middleware 是唯一 evidence writer。Sandbox 每個 action 產生一個 tracker summary、一個 session line與 attempt/result 兩筆 audit。
- 無 sandbox 仍套用 context/workspace/policy/confirmation/limit 並保留 in-memory summaries，但沒有 durable session/audit；不能把 audit 空檔解讀成沒有 action。
- `policy_version="v0.9-unversioned"` 只是 snapshot label。v0.9.3 必須接續 `policy-trust-boundary`，處理 project policy restrict-only trust boundary、versioned schema 與未知欄位 fail-fast，不得在 v0.9.2 偷做。
- v0.9.4 audit integrity、v0.9.5 principal、v0.9.6 authentication、v0.9.7 grant、v0.9.8 decision matrix 與 v1.0-alpha Trust Tier runtime 均未提前實作。
- Release branch 待辦：只有 `release/0.9.2` 可更新 `pyproject.toml` 與 `src/council_agent/__init__.py`，兩處需一致；本 feature branch不得 bump 或 tag。

## v0.9.3 交接狀態

- Runtime／regression evidence：[`v0.9.3-policy-trust-boundary-evidence.md`](v0.9.3-policy-trust-boundary-evidence.md)；SEC-P0-004／V1-004 已由 strict `schema_version: 1`、unknown／typo fail-fast、restrict-only precedence、versioned context snapshot 與 policy-file sentinel tests 關閉。
- Breaking migration：既有 unversioned `council.policy.yaml` 必須新增 integer `schema_version: 1`，並移除 `allowed_commands`／`denied_commands`／`denied_paths` 以外的所有欄位。String `"1"`、future version、`trust_tier`、`grant`、`scope`、拼錯及任意未知欄位會拒絕整份檔案，不再忽略。
- Project policy 只作限制：allowlist 是 built-in／後續 gates 的 intersection，deny/path 只作 union；不能建立 principal、authentication、grant、Trust Tier 或 confirmation bypass。`policy_version` snapshot label 是 `builtin` 或 `project-policy/v1`。
- Root／nested `council.policy.yaml` 是 built-in denied path；public filesystem tools 與受支援 shell path action 不能直接讀寫／刪除，且拒絕無副作用。這不是 OS sandbox：`run_tests` 中的專案程式碼、host user 或外部程序仍可能改檔。
- v0.9.4 接續 audit control-plane／redaction／sequence；workspace 外 user-owned trust grant store 仍只屬 v0.9.7。不得將 project policy 當 grant store，也不得提前實作 Trust Tier。
- OpenSpec delta 已同步並歸檔至 `openspec/changes/archive/2026-08-11-policy-trust-boundary/`；17/17 tasks complete。
- Post-archive `./scripts/check.sh`：334 passed；no active changes；main specs 5/5 strict passed。
- Release branch 待辦：只有 `release/0.9.3` 可更新 `pyproject.toml` 與 `src/council_agent/__init__.py`，兩處需一致；本 feature branch不得 bump 或 tag。

## v0.9.4 交接狀態

- Runtime／regression evidence：[`v0.9.4-audit-integrity-evidence.md`](v0.9.4-audit-integrity-evidence.md)；V1-005／SEC-P0-007／SEC-P1-003 的 v0.9.4 substrate scope由 control-plane deny、recursive redaction、schema-v1 sequence/canonical ID及 exact attempt/result linkage關閉。
- Root／nested `.council` 與 project policy是 built-in denied paths；public filesystem與受支援 shell path action不可將 audit/session/config/reserved auth/grant control plane當作 target。Project policy不能移除；POSIX mode只作 defense in depth。
- Audit/session persistence共用 sensitive-key與 common-token content redaction，且 redaction早於 truncation。Legacy evidence可 sanitized read/export但 status固定 `legacy_unverified`。
- New audit event含 `schema_version: 1`、serialized sequence、content-derived `sha256:` event ID；strict load偵測 gap、duplicate、reorder、partial/truncated line、field mutation及 broken attempt/result correlation。CLI顯示 integrity status並拒絕 invalid export。
- 這不是完整 hash chain：沒有 predecessor link、external signature/checkpoint、immutable sink或 tail/whole-log replacement保證。Host owner/root、`run_tests` project code與 external process不受 product path deny containment。
- 漸進測試：audit 23 passed；guard/shell/session 101 passed；middleware/CLI/e2e 41 passed。Full／active-change `./scripts/check.sh`：375 passed；changes strict 1/1；main specs strict 5/5。
- OpenSpec delta已同步並歸檔至 `openspec/changes/archive/2026-08-11-audit-integrity/`；15/15 tasks complete。Post-archive `./scripts/check.sh`：375 passed；no active changes；main specs 5/5 strict passed。
- v0.9.5 從 `principal-scope` 開始：provider API key不是 authorization principal；不得把 v0.9.4 event/session ID當 authentication、scope或 grant。
- Release：版本 bump僅在 `release/0.9.4` 完成；feature已 merge，tag `v0.9.4` 已建立。本 feature branch本身未做 version bump或 tag。
